#!/usr/bin/bash

CYAN='[1;36m'
NO_COLOR='[0m'
LABEL=./backend-flask/bin/db-drop
printf "${CYAN}== ${LABEL}${NO_COLOR}"

NO_DB_CONNECTION_URL=$(sed 's/\/crudder//g' <<< $CONNECTION_URL)
psql $NO_DB_CONNECTION_URL -c "drop database cruddur;"